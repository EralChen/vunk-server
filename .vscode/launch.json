{
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Run Script: server:dev",
      "request": "launch",
      "command": "pnpm run server:dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}
